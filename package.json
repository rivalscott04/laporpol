{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "npm run copy-pdfjs && vite build",
        "copy-pdfjs": "rm -rf public/pdfjs && mkdir -p public/pdfjs && curl -fsSL \"https://github.com/mozilla/pdf.js/releases/download/v3.11.174/pdfjs-3.11.174-dist.zip\" -o /tmp/pdfjs-dist.zip && unzip -qo /tmp/pdfjs-dist.zip -d public/pdfjs",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
